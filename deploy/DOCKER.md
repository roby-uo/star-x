# star-X Docker image

Published image:

```text
ghcr.io/roby-uo/star-x:latest
```

Use `deploy/docker-compose.yml` together with a private `.env` file. Never publish `.env`, database volumes, or application data.
